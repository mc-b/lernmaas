#
#   Konfigurationsstring pro HOSTNAME.
#
#   Entscheided welche Module nach /etc/cloud/cloud.cfg.d/ kopiert werden um sie nachher von cloud-init ausfuehren zu lassen
#

# einfache Installation von Service wie z.B. apache
apache=nfs.sh apache.sh

# Docker und Kubernetes Varianten
docker=nfs.sh docker.sh
k3s=nfs.sh k3s.sh
openshift=docker.sh openshift.sh